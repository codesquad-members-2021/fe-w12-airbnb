(function (window, document) {
  'use strict';

  const $logoButton = document.querySelector('#logo--button');
  $logoButton.addEventListener('click', () => re);

  let re = location.reload;
})(window, document);
